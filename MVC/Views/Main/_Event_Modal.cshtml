@model MVC.Models.Schedule

@{
    Layout = "";
}

<div>
    <div>
        <dl class="dl-horizontal">
            <dt>
                @Html.DisplayNameFor(model => model.start)
            </dt>

            <dd>
                @Model.start.ToShortDateString()
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.end)
            </dt>

            <dd>
                @Model.end.AddDays(-1).ToShortDateString()
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.discription)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.discription)
            </dd>
            @if (Model.title != "請假")
            {
                <dt>
                    @Html.DisplayNameFor(model => model.filepath)
                </dt>
                <dd>
                    @Html.ActionLink("點擊查看", "GetReport", new { id = Model.id }, new { @class = "btn btn-info btn-sm", @target = "_blank" })
                </dd>
                <dt>
                    取消指派
                </dt>
                <dd>
                    @Html.ActionLink("點擊取消", "Cancel", new { id = Model.id }, new { @class = "btn btn-danger btn-sm" })
                </dd>
            }

        </dl>
    </div>
</div>